# Root Environment Variables Template
# Copy this file to .env and fill in the values

# AWS Configuration (for deployment)
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=your-account-id

# Environment
NODE_ENV=development

# Note: Each package may have its own .env file
# - packages/client/.env (VITE_ prefixed variables)
# - packages/server/.env (API configuration)
# See individual .env.template files in each package